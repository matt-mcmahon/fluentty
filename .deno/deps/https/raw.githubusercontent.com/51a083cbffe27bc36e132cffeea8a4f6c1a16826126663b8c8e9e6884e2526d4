/**
 * ```
 * isNumber :: a => boolean
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns `true` if __a__ has type `number`.
 *
 */
export const isNumber = (a: unknown): a is number => typeof a === "number";
