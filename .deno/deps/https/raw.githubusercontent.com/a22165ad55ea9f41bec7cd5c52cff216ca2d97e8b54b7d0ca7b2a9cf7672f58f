/**
 * ```
 * identity :: a => a
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns it's argument, unmodified.
 *
 */
export const identity = <A>(a: A): A => a;
