/**
 * ```
 * init :: as => as
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns all but the last element in an array.
 *
 */
export const init = <A>(as: A[]): A[] => as.slice(0, as.length - 1);
