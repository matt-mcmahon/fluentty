/**
 * ```
 * isDefined :: a => boolean
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns `false` if a is `undefined` or `null`, otherwise return `true`.
 *
 */
export const isArray = Array.isArray;
