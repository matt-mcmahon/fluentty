/**
 * ```
 * merge :: a => b => c
 * ```
 * -----------------------------------------------------------------------------
 *
 * Performs a shallow merge of two objects.
 *
 */
export const merge = <A>(a: A) => <B>(b: B): A & B => Object.assign({}, a, b);
