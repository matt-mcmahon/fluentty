/**
 * ```
 * take :: (n, as) => as
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns the first _n_ elements of the given list.
 *
 */
export const take = (n: number) => <A>(as: readonly A[]): A[] => as.slice(0, n);
